/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: "exciting-jackal-863.convex.cloud",
            },
        ],
    },
};

export default nextConfig;
